# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://116.193.129.229:8091
# NEXT_PUBLIC_API_BASE_URL=http://192.168.0.105:8001

# Request timeout in milliseconds
NEXT_PUBLIC_API_TIMEOUT_MS=30000

# Cookie / localStorage key where the access token is stored
NEXT_PUBLIC_AUTH_TOKEN_KEY=hrms_access_token

# App environment label (development | staging | production)
NEXT_PUBLIC_APP_ENV=development

# Auth API login (POST /api/v1/auth/login):
#   user_name + password (seeded password: Password@123)
#   users: Suvendu | hr.executive | payroll.executive | employee | reports